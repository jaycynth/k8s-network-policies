apiVersion: v1
kind: Service
metadata:
  name: mysql-service
spec:
  type: ClusterIP  # Internal access within the cluster
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: mysql  # Must match the labels defined in the MySQL deployment
