apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-microservice-to-mysql
spec:
  endpointSelector:
    matchLabels:
      app: user
  egress:
  - toEndpoints:
    - matchLabels:
        app: mysql
